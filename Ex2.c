/* 2. Escreva uma funcao que some dois inteiros e retorne o valor da soma. */

#include <stdio.h>
int
main ()
{
  int x = 1;
  int y = 2;
  int soma = 0;
  soma = x + y;
  printf ("%d", soma);
  return (0);
}
